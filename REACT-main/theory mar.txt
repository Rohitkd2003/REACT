1️⃣ let / const
🔹 Definition:
let → variable declare karayla, jyachi value change karta yete
const → variable declare karayla, jyachi value change nahi karta yet
🔹 Advantages:
var peksha block scope aste ({ } madhye limit)
bugs kami hotat
clean & secure code
🔹 Uses:
let → jithe value change honar ahe
const → jithe value constant ahe (config, fixed values)
-----------------------------------------------------------------------

2️⃣ Arrow Functions (=>)
🔹 Definition:
Short syntax for function likhnyasathi.
🔹 Advantages:
code short & readable
this keyword automatically handle karto
callbacks sathi perfect
🔹 Uses:
array methods (map, filter)
React components
small utility functions
-----------------------------------------------------------------------------------------------------

3️⃣ Destructuring
🔹 Definition:
Object / array madhun values direct variables madhe extract karne.
🔹 Advantages:
less code
clean syntax
easy access to data
🔹 Uses:
API response handle karayla
props extract (React)
object values quickly gheyla
-------------------------------------------------------------------------------------------------------------

4️⃣ Spread / Rest Operator (...)
🔹 Spread (...) – Definition:
Array/object copy ya merge karayla use hoto.
🔹 Rest (...) – Definition:
Multiple values ekach variable madhe collect karayla.
🔹 Advantages:
immutability maintain
flexible functions
clean array/object handling
🔹 Uses:
array copy
object merge
variable arguments
-----------------------------------------------------------------------------

5️⃣ map() / filter() / reduce()
🔹 map()
👉 Array madhlya pratyek element var operation karto ani new array return karto.
Use: transformation (UI data, calculations)
🔹 filter()
👉 Condition true asnare elements filter karto.
Use: search, remove unwanted data
🔹 reduce()
👉 Array la single value madhe convert karto.
------------------------------------------------------------------------

--------------------------------------------------------------------------

🟦 Day 2 – TypeScript Basics


1️⃣ Basic Types
🔹 Definition:
TypeScript madhe variables la type define karta yeto (number, string, boolean, etc.)

🔹 Common Types:
number

string

boolean

any

null, undefined

🔹 Advantages:
compile time la error kalto

code predictable hoto

runtime bugs kami hotat

🔹 Uses:
strict data handling

large applications madhe safety

let age: number = 22;
let name: string = "Rohit";
let isActive: boolean = true;

--------------------------------------------------------------------------

2️⃣ Arrays & Objects
🔹 Arrays – Definition:
Array madhle elements same type che astat.

🔹 Advantages:
data consistency

wrong data insert honar nahi

🔹 Uses:
list of users, products, ids

let numbers: number[] = [1, 2, 3];
let names: Array<string> = ["Rohit", "Amit"];


🔹 Objects – Definition:
Object madhe keys ani tyanchya types define karta yetat.

🔹 Advantages:
structured data

auto-suggestions (intellisense)

🔹 Uses:
API data

user profiles

let user: {
  name: string;
  age: number;
} = {
  name: "Rohit",
  age: 22
};

--------------------------------------------------------------------------

3️⃣ Union Types / Optional Properties
🔹 Union Types (|) – Definition:
Ek variable multiple types accept karu shakto.

🔹 Advantages:
flexibility with safety

real-world data handling

🔹 Uses:
API response

form inputs

let id: number | string;
id = 101;
id = "A101";
🔹 Optional (?) – Definition:
Property optional asel tar ? vapartat.

🔹 Advantages:
error yet nahi

partial data handle karta yeto

🔹 Uses:
optional fields (email, phone)

let user: {
  name: string;
  email?: string;
};

--------------------------------------------------------------------------

4️⃣ Function Typing
🔹 Definition:
Function madhe parameters ani return type define karta yetat.

🔹 Advantages:
correct input-output ensure hoto

runtime errors kami hotat

🔹 Uses:
utility functions

API logic

function add(a: number, b: number): number {
  return a + b;
}
Arrow function madhe:

const multiply = (a: number, b: number): number => a * b;

--------------------------------------------------------------------------


5️⃣ Practice: JS → TS Conversion
🔹 JavaScript Code:
function greet(name) {
  return "Hello " + name;
}
🔹 TypeScript Code:
function greet(name: string): string {
  return "Hello " + name;
}
🔹 JS Array Function:
const sum = (a, b) => a + b;
🔹 TS Version:
const sum = (a: number, b: number): number => a + b;

--------------------------------------------------------------------------
--------------------------------------------------------------------------